# Django is needed to run Django
Django

# gunicorn is needed by Heroku to launch the web server
gunicorn

# django-heroku needed to configure Django app to work on Heroku
django-heroku

# django-bootstrap-v5 needed for Heroku
django-bootstrap-v5